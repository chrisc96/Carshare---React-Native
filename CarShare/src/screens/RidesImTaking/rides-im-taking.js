import React, { Component } from 'react';
import { View, Text } from 'react-native';
import styles from './rides-im-taking-styles'
import { RidesTakingHeaderTitle } from '../../config/constants';
import { headerTextColour, normalFontWeight, headerBgColor } from '../../config/global-styles';
import { Container, Navbar } from 'navbar-native';

export default class RidesImTaking extends Component {

  render() {
    return (
      <View>
        <Text>Tried to get header working, didn't work :(</Text>
      </View>
    );
  }
}
