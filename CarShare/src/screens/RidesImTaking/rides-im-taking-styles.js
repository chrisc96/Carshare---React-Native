import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    listingsContainer: {
        width: '100%',
        height: '90%',
        paddingRight: 15
    },
    container: {
        flex: 1
    }
});